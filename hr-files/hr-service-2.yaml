apiVersion: v1
kind: Service
metadata:
  labels:
    app: hr-app
  name: hr-service-2
  namespace: hr
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: hr-app
  type: ClusterIP
