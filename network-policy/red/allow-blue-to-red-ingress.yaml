apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-blue-to-red-ingress
  namespace: color
spec:
  podSelector:
    matchLabels:
      run: red
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          run: blue
    ports:
    - protocol: TCP
      port: 80


